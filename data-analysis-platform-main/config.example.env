# 智能数据分析平台 - 环境配置示例
# 复制此文件为 .env 并填入您的实际配置

# LLM API配置
# 阿里云Dashscope API密钥 (使用dashscope时必需)
DASHSCOPE_API_KEY=sk-your-dashscope-api-key-here

# OpenAI API密钥 (使用openai时必需)
OPENAI_API_KEY=sk-your-openai-api-key-here

# LLM提供商配置 (可选，默认dashscope)
LLM_PROVIDER=dashscope

# LLM模型配置 (可选)
LLM_MODEL=qwen-turbo
LLM_TIMEOUT=300
LLM_MAX_TOKENS=4000
LLM_TEMPERATURE=0.7
LLM_TOP_P=0.8

# 系统配置 (可选)
DEFAULT_CONFIDENCE_THRESHOLD=0.8
DEFAULT_OUTPUT_FORMAT=markdown
DEFAULT_OUTPUT_DIR=./output

# 示例用法：
# 1. 复制此文件为 .env
# 2. 填入您的实际API密钥
# 3. 运行程序时会自动读取环境变量
#
# 注意：请勿将包含真实API密钥的 .env 文件提交到代码仓库
